class SlidersModel {
  final String image;
  final SliderText text;
  SlidersModel({
    this.image,
    this.text,
  });
}

class SliderText {
  final String text;
  final String direction;
  SliderText({
    this.text,
    this.direction,
  });
}
